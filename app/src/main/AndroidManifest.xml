<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Truvideosdkcore">
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Truvideosdkcore">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.files.FilesActivity"
            android:exported="true"
            android:label="Files"
            android:theme="@style/Theme.Truvideosdkcore" />

        <activity
            android:name=".ui.csv_viewer.CsvViewerActivity"
            android:exported="true"
            android:label="CSV Viewer"
            android:theme="@style/Theme.Truvideosdkcore" />

        <activity
            android:name=".ui.txt_viewer.TxtViewerActivity"
            android:exported="true"
            android:label="TXT Viewer"
            android:theme="@style/Theme.Truvideosdkcore" />
    </application>
</manifest>